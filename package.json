{
  "name": "katy-decorah",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "test": "scss-lint _sass/*.scss && htmlproofer ./_site && tape test/*.js | tap-spec"
  },
  "dependencies": {
    "scsslint": "0.0.3"
  },
  "devDependencies": {
    "js-yaml": "^2.1.3",
    "tap-spec": "~4.1.1",
    "tape": "^4.6.3"
  }
}
